# MedReminderApp - Environment Template
# Instructions: Copy this file to .env and fill in your own credentials

# --- Backend Email Settings (SMTP) ---
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password

# --- Frontend API Configuration ---
# Set this to your local computer's IP address so the mobile app can reach the server
EXPO_PUBLIC_API_URL=http://YOUR_LOCAL_IP:5000

# --- Backend Debug Settings ---
FLASK_ENV=development
FLASK_DEBUG=1
